apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - grafanadashboard.yaml

configMapGenerator:
  - name: operator-chaos-grafana-dashboard
    files:
      - dashboard.json
    options:
      disableNameSuffixHash: true
      labels:
        grafana_dashboard: "1"
