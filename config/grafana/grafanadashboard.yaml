apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: operator-chaos-overview
  labels:
    app.kubernetes.io/name: operator-chaos-controller
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  configMapRef:
    name: operator-chaos-grafana-dashboard
    key: dashboard.json
