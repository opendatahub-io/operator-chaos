apiVersion: v1
kind: Service
metadata:
  name: operator-chaos-controller
  labels:
    app.kubernetes.io/name: operator-chaos-controller
spec:
  selector:
    app.kubernetes.io/name: operator-chaos-controller
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
