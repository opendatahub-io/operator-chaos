apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: odh-chaos-dashboard-data
  labels:
    app.kubernetes.io/name: odh-chaos-dashboard
    app.kubernetes.io/part-of: odh-platform-chaos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
