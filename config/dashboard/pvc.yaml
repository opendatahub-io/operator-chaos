apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: operator-chaos-dashboard-data
  labels:
    app.kubernetes.io/name: operator-chaos-dashboard
    app.kubernetes.io/part-of: operator-chaos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
