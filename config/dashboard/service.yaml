apiVersion: v1
kind: Service
metadata:
  name: odh-chaos-dashboard
  labels:
    app.kubernetes.io/name: odh-chaos-dashboard
    app.kubernetes.io/part-of: odh-platform-chaos
spec:
  selector:
    app.kubernetes.io/name: odh-chaos-dashboard
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
